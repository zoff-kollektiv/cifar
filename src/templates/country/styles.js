import css from 'styled-jsx/css';

import { colors } from '../../tokens';

export default css`
  .title {
    color: ${colors.blue};
    font-size: 3.34rem;
    font-weight: 700;
    line-height: 1.12;
    margin-bottom: 0;
    margin-top: 3.5rem;
  }

  .intro {
    color: ${colors.blue};
    font-size: 1.5rem;
    font-weight: 500;
    line-height: 1.58;
    margin-bottom: 4rem;
  }
`;
