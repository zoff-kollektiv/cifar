import css from 'styled-jsx/css';

import { colors } from '../../tokens';

export default css`
  .about {
    color: ${colors.blue};
    font-size: 1.5rem;
    line-height: 1.45;
    padding-bottom: 8rem;
  }

  .title {
    font-size: 2.43rem;
    font-weight: 700;
    line-height: 1.2;
    margin-top: 6rem;
  }
`;
