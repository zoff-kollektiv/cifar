import css from 'styled-jsx/css';

import { colors } from '../../tokens';

export default css`
  footer {
    background-color: ${colors.blue};
    color: white;
    padding-bottom: 6rem;
    padding-top: 2rem;
  }
`;
