import React from 'react';

import Constraint from '../constraint';

import styles from './styles';

export default () => (
  <footer>
    <style jsx>{styles}</style>

    <Constraint>Imprint</Constraint>
  </footer>
);
